# Music Attribution Scaffold — Environment Variables
# Copy to .env and fill in your values: cp .env.example .env
# .env is gitignored and NEVER committed.

# ── Core Application ────────────────────────────────────────────────
DATABASE_URL=postgresql+psycopg://musicattr:musicattr_dev@localhost:5432/music_attribution  # pragma: allowlist secret
VALKEY_URL=redis://localhost:6379
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=http://localhost:3000
LOG_LEVEL=INFO
ENVIRONMENT=development
ATTRIBUTION_SEED=42

# ── External Data APIs ──────────────────────────────────────────────
MUSICBRAINZ_USER_AGENT=MusicAttributionScaffold/1.0 (your@email.com)
DISCOGS_TOKEN=your-discogs-personal-access-token

# ── Text Agent (PydanticAI) ─────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...
LLM_PROVIDER=anthropic
LLM_MODEL=claude-haiku-4-5
ATTRIBUTION_AGENT_MODEL=anthropic:claude-haiku-4-5

# ── Voice Agent (Pipecat) ──────────────────────────────────────────
# All VOICE_* vars are optional — defaults use fully open-source stack.
# See docs/tutorials/voice-agent-implementation.md for provider details.

# Provider selection (whisper/deepgram/assemblyai, piper/elevenlabs/cartesia/kokoro)
# VOICE_STT_PROVIDER=whisper
# VOICE_TTS_PROVIDER=piper
# VOICE_TRANSPORT=websocket

# LLM for voice pipeline (OpenAI-compatible API)
# VOICE_LLM_API_KEY=sk-...
# VOICE_LLM_MODEL=gpt-4o-mini

# Commercial API keys (only needed when switching from open-source defaults)
# VOICE_DEEPGRAM_API_KEY=your-deepgram-key
# VOICE_ELEVENLABS_API_KEY=your-elevenlabs-key
# VOICE_CARTESIA_API_KEY=your-cartesia-key
# VOICE_ASSEMBLYAI_API_KEY=your-assemblyai-key
# VOICE_DAILY_API_KEY=your-daily-key

# Persona management (optional)
# VOICE_PERSONA_ENABLED=false
# VOICE_LETTA_BASE_URL=http://localhost:8283
# VOICE_MEM0_API_KEY=your-mem0-key

# Drift detection & guardrails (optional)
# VOICE_DRIFT_MONITORING=false
# VOICE_GUARDRAILS_ENABLED=false

# ── Frontend (Next.js) ─────────────────────────────────────────────
# NEXT_PUBLIC_API_URL=http://localhost:8000
# NEXT_PUBLIC_POSTHOG_KEY=phc_...
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
