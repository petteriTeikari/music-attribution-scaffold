"""Evaluation test suite — LLM-based persona fidelity tests.

These tests require LLM API access and are marked with
``@pytest.mark.persona_eval``. Run with::

    pytest tests/eval/ -m persona_eval

They are NOT part of the regular CI test suite.
"""
