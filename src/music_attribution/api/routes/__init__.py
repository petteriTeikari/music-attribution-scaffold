"""API route sub-package.

Contains individual router modules for each endpoint group:

* ``attribution`` — attribution CRUD and search endpoints
* ``permissions`` — MCP-compatible permission queries
* ``health`` — liveness probe
* ``metrics`` — Prometheus metrics exposition
"""

from __future__ import annotations
